# Regions to leave UNWARPED, to preserve intentional baka-world-map design.
#
# The automatic warp assumes every distortion is an error vs. real geography.
# Where a distortion is INTENTIONAL (a deliberately reshaped island/country),
# list it here so the warp leaves it untouched. Edges are feathered so there
# is no seam with the warped surroundings.
#
# Rectangles in normalized frame coords (0,0 = top-left, 1,1 = bottom-right).
# columns: name,x0,y0,x1,y1
#
# --- intentional baka-world-map design (per author: "仕様") -----------------
# These shapes are deliberately drawn that way, so the warp must NOT pull them
# toward real geography. Boxes cover where the features are PAINTED.
australia,0.785,0.590,0.925,0.755
japan_korea_shikoku,0.845,0.200,0.915,0.400
#
# --- example (commented out) -----------------------------------------------
#fictional_island,0.40,0.30,0.46,0.36
